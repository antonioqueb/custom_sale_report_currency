<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="sale_order_report_currency_symbol" inherit_id="sale.report_saleorder_document">
        <!-- Agregar símbolo al precio unitario en las líneas de pedido -->
        <xpath expr="//td[@name='price_unit']" position="replace">
            <td class="text-end" name="price_unit">
                <span t-field="line.price_unit" t-options="{'widget': 'monetary', 'display_currency': o.currency_id}" />
            </td>
        </xpath>

        <!-- Agregar símbolo al subtotal en las líneas de pedido -->
        <xpath expr="//td[@name='price_subtotal']" position="replace">
            <td class="text-end" name="price_subtotal">
                <span t-field="line.price_subtotal" t-options="{'widget': 'monetary', 'display_currency': o.currency_id}" />
            </td>
        </xpath>

        <!-- Agregar símbolo al total general -->
        <xpath expr="//span[@t-field='o.amount_total']" position="replace">
            <span t-field="o.amount_total" t-options="{'widget': 'monetary', 'display_currency': o.currency_id}" />
        </xpath>
    </template>
</odoo>
